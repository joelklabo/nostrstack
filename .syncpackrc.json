{
  "versionGroups": [
    {
      "label": "Use workspace protocol for internal packages",
      "packages": ["**"],
      "dependencies": ["@nostrstack/*"],
      "dependencyTypes": ["dev", "prod", "peer"],
      "pinVersion": "workspace:*"
    },
    {
      "label": "Allow different fastify versions (api needs 4.x for plugins)",
      "dependencies": ["fastify"],
      "isIgnored": true
    }
  ]
}
